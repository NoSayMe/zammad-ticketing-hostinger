# Registry namespace (local development uses a dummy namespace)
DOCKER_REGISTRY=local

# Public domain used by NGINX/Certbot
REMOTE_DOMAIN=localhost

# Elasticsearch JVM options
ES_JAVA_OPTS=-Xms512m -Xmx512m

# Postgres credentials (used by the Postgres container)
POSTGRES_DB=zammad
POSTGRES_USER=zammad
POSTGRES_PASSWORD=zammad

# Optional: Certbot email (used by deploy-script.sh when bootstrapping certs)
# CERTBOT_EMAIL=admin@example.com

# Placeholders for future integrations (Microsoft 365, etc.)
# M365_TENANT_ID=
# M365_CLIENT_ID=
# M365_CLIENT_SECRET=
# M365_SMTP_USER=
# M365_SMTP_PASSWORD=
# M365_SHARED_MAILBOX=
